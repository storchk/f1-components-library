import { Meta, Typeset } from '@storybook/blocks'

<Meta title="General/Typography" />

export const typography = {
  type: {
    primary: 'SoDo Sans',
  },
  weight: {
    thin: '100',
    light: '300',
    regular: '400',
    semiBold: '600',
    bold: '700',
    black: '900',
  },
  size: {
    s1: 12,
    s2: 14,
    s3: 16,
    m1: 20,
    m2: 24,
    m3: 28,
    l1: 32,
    l2: 40,
    l3: 48,
  },
}

export const SampleText = 'The quick brown fox jumps over the lazy dog'

# Typography

**Font:** SoDo Sans

**Weights:** 100(thin), 300(light), 400(regular), 800(semiBold), 700(bold), 900(black)

<Typeset
  fontSizes={[
    Number(typography.size.s1),
    Number(typography.size.s2),
    Number(typography.size.s3),
    Number(typography.size.m1),
    Number(typography.size.m2),
    Number(typography.size.m3),
    Number(typography.size.l1),
    Number(typography.size.l2),
    Number(typography.size.l3),
  ]}
  fontWeight={typography.weight.regular}
  sampleText={SampleText}
  fontFamily={typography.type.primary}
/>
